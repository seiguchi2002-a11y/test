# 智享物递机器人厂商接口需求文档

版本：v1.0  
用途：请机器人厂商基于本文档提供对应开放接口、回调、状态码、测试环境和联调资料。  
业务背景：我方正在建设“智享物递”模块，包含取餐送物、机器人配送订单、订单状态展示、订单详情、我的订单、特惠购卡和积分兑换等功能。其中机器人相关能力主要用于“取餐送物”流程。

## 1. 我方业务流程

### 1.1 取餐送物主流程

1. 用户进入“取餐送物”页面。
2. 系统查询当前是否有可用机器人。
3. 用户填写取餐号/取件码。
4. 用户选择或搜索收件地址。
5. 用户提交配送订单。
6. 系统创建机器人配送任务。
7. 机器人前往取件点。
8. 用户或商户放入物品/餐品。
9. 机器人前往收件点。
10. 收件人开舱取件。
11. 任务完成，订单状态更新。

### 1.2 我方需要展示的信息

- 是否有空闲机器人
- 机器人当前位置
- 机器人电量和状态
- 配送路径
- 预计到达时间
- 当前任务阶段
- 取件/放件/配送/收件状态
- 任务异常原因
- 订单完成或失败结果

## 2. 对接模式要求

请厂商明确支持哪种对接模式：

| 模式 | 说明 | 是否支持 |
| --- | --- | --- |
| 厂商自动调度 | 我方只创建任务，厂商自动分配机器人 | 请确认 |
| 我方指定机器人 | 我方先查询可用机器人，再指定机器人创建任务 | 请确认 |
| 混合调度 | 可自动调度，也可指定机器人 | 请确认 |

建议支持：厂商自动调度 + 我方查询可用机器人。

## 3. 鉴权与安全要求

请提供以下能力：

1. API 鉴权方式：API Key、Token、OAuth、签名等。
2. 回调签名校验方式。
3. IP 白名单配置方式。
4. 请求时间戳和 nonce 防重放机制。
5. 接口幂等机制，尤其是创建任务、取消任务、开舱。
6. 错误码和错误信息规范。
7. 接口限流规则。
8. 测试环境和生产环境地址。

## 4. 机器人资源接口

### 4.1 查询可用机器人

用途：用户进入取餐送物或提交订单前，判断是否有可用机器人。

请求参数建议：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| requestId | string | 是 | 请求唯一 ID |
| scene | string | 是 | 场景：meal、item |
| pickupPointId | string | 是 | 取件点位 ID |
| deliveryPointId | string | 否 | 收件点位 ID |
| buildingId | string | 否 | 楼栋 ID |
| floor | string | 否 | 楼层 |

返回字段建议：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| available | boolean | 是否有可用机器人 |
| estimatedWaitSeconds | integer | 预计等待时间 |
| robots | array | 可用机器人列表 |

机器人字段建议：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| robotId | string | 机器人唯一 ID |
| robotName | string | 机器人名称 |
| status | string | 状态 |
| batteryLevel | integer | 电量百分比 |
| currentPointId | string | 当前点位 ID |
| currentLocationName | string | 当前位置名称 |
| distanceMeters | integer | 距离取件点距离 |
| supportCompartmentCount | integer | 支持舱门数量 |

### 4.2 查询机器人详情

用途：后台查看机器人详情或订单详情展示机器人信息。

需要返回：

- 机器人 ID
- 机器人名称
- 当前状态
- 电量
- 是否在线
- 当前楼栋/楼层/位置
- 舱门数量
- 舱门状态
- 当前任务 ID
- 故障状态
- 最近上报时间

### 4.3 查询机器人实时位置

用途：前端地图展示和订单详情展示。

需要返回：

- 机器人 ID
- 坐标类型
- x/y 坐标或经纬度
- 楼栋
- 楼层
- 位置描述
- 朝向，若支持
- 速度，若支持
- 上报时间

## 5. 点位、地图和路线接口

### 5.1 查询可配送点位

用途：我方维护或同步取件点、收件点、餐厅、楼栋、楼层等地址。

请提供：

- 点位 ID
- 点位名称
- 点位类型：餐厅、前台、房间、公共点、充电桩等
- 楼栋
- 楼层
- 室内地图坐标
- 是否可作为取件点
- 是否可作为收件点
- 是否启用

### 5.2 地址/点位搜索

如厂商侧已维护点位库，请提供关键词搜索接口。

### 5.3 路线规划接口

用途：下单前判断能否配送，以及订单页展示路线。

请求参数建议：

- 起点点位 ID
- 终点点位 ID
- 场景

返回字段建议：

- 是否可达
- 路径点集合
- 距离
- 预计耗时
- 不可达原因
- 涉及楼栋/楼层
- 是否需要电梯/门禁/闸机

### 5.4 地图数据

请确认是否可以提供：

- 室内地图底图
- 地图坐标系说明
- 楼层切换规则
- 点位坐标
- 路径线坐标
- 地图更新方式

## 6. 机器人任务接口

### 6.1 创建配送任务

用途：我方订单创建后，向厂商创建机器人任务。

请求字段建议：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| requestId | string | 是 | 请求唯一 ID，支持幂等 |
| orderNo | string | 是 | 我方订单编号 |
| scene | string | 是 | meal、item |
| pickupPointId | string | 是 | 取件点位 ID |
| deliveryPointId | string | 是 | 收件点位 ID |
| pickupCode | string | 否 | 取餐号/取件码 |
| pickupContactName | string | 否 | 取件联系人 |
| pickupContactPhone | string | 否 | 取件联系电话 |
| receiverName | string | 是 | 收件人 |
| receiverPhone | string | 是 | 收件手机号 |
| remark | string | 否 | 备注 |
| robotId | string | 否 | 指定机器人 ID |
| callbackUrl | string | 是 | 任务状态回调地址 |

返回字段建议：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| robotTaskId | string | 厂商任务 ID |
| robotId | string | 分配的机器人 ID |
| status | string | 任务状态 |
| estimatedPickupTime | string | 预计到达取件点时间 |
| estimatedDeliveryTime | string | 预计送达时间 |
| message | string | 返回说明 |

### 6.2 查询任务详情

用途：我方主动查询任务状态，作为回调丢失时的补偿。

请求参数：

- robotTaskId 或 orderNo

返回字段需要包含：

- 厂商任务 ID
- 我方订单编号
- 机器人 ID
- 当前任务状态
- 当前步骤
- 起点和终点
- 机器人当前位置
- 舱门状态
- 预计到达时间
- 异常信息
- 状态时间线

### 6.3 取消任务

用途：用户取消订单、后台取消订单、异常终止。

请求字段建议：

- requestId
- robotTaskId
- orderNo
- reasonCode
- reasonText

返回需说明：

- 是否取消成功
- 当前任务状态
- 是否产生费用
- 失败原因

请明确哪些状态允许取消，哪些状态不允许取消。

### 6.4 重新调度任务

用途：机器人故障、离线、被阻挡、电量不足时，我方后台可触发重新派单。

请确认是否支持：

- 原任务取消后重新创建
- 同一任务更换机器人
- 异常任务人工接管后恢复

## 7. 舱门控制接口

### 7.1 打开舱门

用途：用户/商户放件、收件人取件、后台人工开舱。

请求字段建议：

- requestId
- robotTaskId
- orderNo
- robotId
- action：put_item、take_item、admin_open
- compartmentNo
- verifyCode
- operatorId
- operatorType：user、admin、merchant

返回字段建议：

- success
- compartmentNo
- compartmentStatus
- message

### 7.2 查询舱门状态

需要返回：

- 舱门编号
- 开关状态
- 是否占用
- 是否异常
- 是否检测到物品
- 最近开关时间

### 7.3 放件确认

用途：用户或商户放入物品后确认。

请确认支持方式：

- 用户点击确认
- 机器人自动检测
- 厂商回调确认

### 7.4 取件确认

用途：收件人取出物品后确认任务完成。

请确认支持方式：

- 用户点击确认
- 舱内检测自动确认
- 关门后自动确认

## 8. 状态回调接口

我方需要厂商在任务状态变化时主动回调。

### 8.1 回调地址

由我方提供：

POST `/api/v1/robot/callbacks/task-status`

### 8.2 回调字段建议

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| eventId | string | 是 | 事件唯一 ID，防重复 |
| robotTaskId | string | 是 | 厂商任务 ID |
| orderNo | string | 是 | 我方订单编号 |
| robotId | string | 是 | 机器人 ID |
| status | string | 是 | 任务状态 |
| eventTime | string | 是 | 事件时间 |
| location | object | 否 | 当前位置 |
| compartment | object | 否 | 舱门信息 |
| reasonCode | string | 否 | 异常原因编码 |
| reasonText | string | 否 | 异常原因说明 |
| rawData | object | 否 | 厂商原始扩展数据 |

### 8.3 必需回调状态

请至少支持以下状态：

| 状态 | 说明 |
| --- | --- |
| task_created | 任务已创建 |
| robot_assigned | 已分配机器人 |
| going_to_pickup | 前往取件点 |
| arrived_pickup | 到达取件点 |
| waiting_put_item | 等待放件 |
| item_loaded | 已放件 |
| going_to_delivery | 前往收件点 |
| arrived_delivery | 到达收件点 |
| waiting_take_item | 等待取件 |
| item_taken | 已取件 |
| completed | 任务完成 |
| canceled | 任务取消 |
| failed | 任务失败 |
| exception | 任务异常 |

### 8.4 异常回调类型

请至少支持以下异常：

- 机器人离线
- 机器人故障
- 电量不足
- 路线不可达
- 被阻挡
- 电梯异常
- 门禁/闸机异常
- 舱门打不开
- 舱门关不上
- 超时未放件
- 超时未取件
- 取件码校验失败
- 人工接管
- 任务被取消

## 9. 取件码和身份校验

请确认以下方案支持情况：

| 校验方案 | 说明 | 是否支持 |
| --- | --- | --- |
| 我方校验 | 用户在我方系统输入验证码，我方校验后调用开舱 | 请确认 |
| 厂商校验 | 我方创建任务时传验证码，厂商在机器人端校验 | 请确认 |
| 混合校验 | 两边均可校验 | 请确认 |

我方建议采用：我方校验后调用厂商开舱接口。

## 10. 后台运维接口

我方后台需要以下能力，请确认是否支持：

1. 查询机器人列表和实时状态。
2. 查询机器人历史任务。
3. 查询指定订单/任务状态日志。
4. 人工取消任务。
5. 人工重新派单。
6. 人工开舱。
7. 标记任务异常已处理。
8. 查询异常任务列表。
9. 查询机器人告警。
10. 禁用/启用某台机器人。

## 11. 计费和费用

如机器人配送涉及厂商计费，请提供：

- 费用预估接口
- 实际费用查询接口
- 取消费用规则
- 异常费用规则
- 费用明细字段
- 对账单接口或对账文件

如厂商不参与用户侧计费，也请明确仅提供配送服务，不参与支付和订单金额计算。

## 12. 接口性能要求

建议目标：

| 场景 | 要求 |
| --- | --- |
| 查询可用机器人 | 1 秒内返回 |
| 创建任务 | 2 秒内返回 |
| 查询任务详情 | 1 秒内返回 |
| 状态回调延迟 | 状态变化后 3 秒内回调 |
| 位置上报频率 | 配送中 3-10 秒一次，可配置 |

## 13. 联调材料清单

请厂商提供以下资料：

1. 开放接口文档。
2. 接口 Base URL，包括测试环境和生产环境。
3. 鉴权参数和签名示例。
4. Postman 集合或 OpenAPI/Swagger 文件。
5. 机器人状态码字典。
6. 任务状态码字典。
7. 异常错误码字典。
8. 点位/地图数据样例。
9. 测试账号。
10. 测试机器人或模拟器。
11. 回调重试规则说明。
12. SLA 和技术支持联系人。

## 14. 需要厂商重点确认的问题

1. 是否支持室内地图、路线和点位数据开放？
2. 是否支持自动调度机器人？
3. 是否支持我方指定机器人？
4. 是否支持机器人实时位置查询和位置回调？
5. 是否支持舱门远程控制？
6. 是否支持舱内物品检测？
7. 是否支持跨楼层配送？
8. 电梯、门禁、闸机是否由厂商负责联动？
9. 用户超时未放件/未取件时机器人如何处理？
10. 机器人异常时是否支持更换机器人继续配送？
11. 是否支持任务状态 webhook 回调？
12. 回调失败后是否会重试，重试多久？
13. 创建任务是否支持幂等？
14. 取消任务有哪些状态限制？
15. 厂商是否参与费用计算和对账？

## 15. 我方提供给厂商的信息

我方可提供：

- 业务订单编号
- 取件点位 ID
- 收件点位 ID
- 取餐号/取件码
- 收件人姓名
- 收件人手机号
- 订单备注
- 状态回调地址
- 后台人工操作人信息

敏感信息要求：手机号等个人信息仅用于配送通知和身份核验，厂商需按双方数据安全协议处理。

