# 智享物递标准接口文档

版本：v1.0  
适用端：小程序/APP、业务后台、机器人调度服务  
说明：本文档基于现有原型功能整理，接口路径、字段和状态码为标准草案，实际开发时可按后端技术规范调整命名。

## 1. 通用约定

### 1.1 请求约定

- 协议：HTTPS
- 数据格式：JSON
- 请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| Authorization | 是 | 用户登录凭证，格式：Bearer token |
| X-Request-Id | 是 | 请求唯一标识，用于幂等和链路追踪 |
| X-Client-Type | 否 | 客户端类型：miniapp、app、admin |
| X-Version | 否 | 客户端版本号 |

### 1.2 响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "requestId": "202605081700000001"
}
```

### 1.3 通用错误码

| code | 说明 |
| --- | --- |
| 0 | 成功 |
| 400001 | 参数错误 |
| 401001 | 未登录或登录失效 |
| 403001 | 无权限 |
| 404001 | 资源不存在 |
| 409001 | 重复请求或状态冲突 |
| 429001 | 请求过于频繁 |
| 500001 | 系统异常 |
| 503001 | 第三方服务不可用 |

## 2. 智享物递首页

### 2.1 获取使用须知

GET `/api/v1/delivery/notice`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| title | string | 标题 |
| content | string | 使用须知富文本/Markdown |
| version | string | 版本号 |
| needConfirm | boolean | 是否需要用户确认 |

### 2.2 确认使用须知

POST `/api/v1/delivery/notice/confirm`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| version | string | 是 | 用户确认的须知版本 |

### 2.3 获取首页配置

GET `/api/v1/delivery/home`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| banners | array | 首页轮播图 |
| quickEntries | array | 快捷入口 |
| benefitCards | array | 特惠卡推荐 |
| pointProducts | array | 积分兑换推荐 |
| serviceStatus | object | 服务状态 |

## 3. 取餐送物

### 3.1 查询服务可用状态

GET `/api/v1/delivery/service-availability`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| scene | string | 是 | 服务场景：meal、item |
| buildingId | string | 否 | 楼栋 ID |
| floor | string | 否 | 楼层 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| available | boolean | 是否可用 |
| reason | string | 不可用原因 |
| serviceTime | object | 服务时间 |
| estimatedWaitMinutes | integer | 预计等待分钟数 |

### 3.2 搜索空闲机器人

GET `/api/v1/robots/available`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pickupAddressId | string | 是 | 取件地址 ID |
| deliveryAddressId | string | 否 | 收件地址 ID |
| scene | string | 是 | meal、item |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| available | boolean | 是否有可用机器人 |
| robots | array | 可用机器人列表 |
| estimatedWaitMinutes | integer | 预计等待时间 |

机器人字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| robotId | string | 机器人 ID |
| robotName | string | 机器人名称 |
| batteryLevel | integer | 电量百分比 |
| locationName | string | 当前位置描述 |
| distanceMeters | integer | 距离取件点距离 |
| status | string | robot 状态 |

### 3.3 地址搜索

GET `/api/v1/addresses/search`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | string | 是 | 搜索关键词 |
| scene | string | 否 | meal、item |
| buildingId | string | 否 | 限定楼栋 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| list | array | 地址列表 |

地址字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| addressId | string | 地址 ID |
| addressName | string | 地址名称 |
| buildingName | string | 楼栋 |
| floor | string | 楼层 |
| roomNo | string | 房间号/点位号 |
| contactName | string | 默认联系人 |
| contactPhone | string | 默认联系电话 |
| mapPoint | object | 地图坐标 |

### 3.4 创建配送订单

POST `/api/v1/delivery/orders`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| scene | string | 是 | meal、item |
| pickupAddressId | string | 是 | 取件地址 ID |
| deliveryAddressId | string | 是 | 收件地址 ID |
| pickupContactName | string | 否 | 取件联系人 |
| pickupContactPhone | string | 否 | 取件联系电话 |
| receiverName | string | 是 | 收件人 |
| receiverPhone | string | 是 | 收件手机号 |
| pickupCode | string | 否 | 取餐号/取件码 |
| remark | string | 否 | 备注 |
| benefitCardId | string | 否 | 使用的特惠卡 ID |
| couponId | string | 否 | 优惠券 ID |
| robotId | string | 否 | 指定机器人，通常由系统调度 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| orderId | string | 业务订单 ID |
| orderNo | string | 订单编号 |
| robotTaskId | string | 机器人任务 ID |
| payRequired | boolean | 是否需要支付 |
| payableAmount | number | 应付金额 |
| status | string | 订单状态 |

### 3.5 获取订单详情

GET `/api/v1/delivery/orders/{orderId}`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| orderId | string | 订单 ID |
| orderNo | string | 订单编号 |
| scene | string | 服务场景 |
| status | string | 业务订单状态 |
| robotTaskStatus | string | 机器人任务状态 |
| robot | object | 机器人信息 |
| pickupAddress | object | 取件地址 |
| deliveryAddress | object | 收件地址 |
| route | object | 路线信息 |
| fee | object | 费用信息 |
| timeline | array | 状态时间线 |
| createdAt | string | 创建时间 |

### 3.6 获取订单实时状态

GET `/api/v1/delivery/orders/{orderId}/status`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| status | string | 业务订单状态 |
| robotTaskStatus | string | 机器人任务状态 |
| currentStep | string | 当前步骤 |
| robotLocation | object | 机器人当前位置 |
| estimatedArrivalTime | string | 预计到达时间 |
| exception | object | 异常信息 |

### 3.7 取消订单

POST `/api/v1/delivery/orders/{orderId}/cancel`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| reasonCode | string | 是 | 取消原因编码 |
| reasonText | string | 否 | 取消原因说明 |

### 3.8 打开舱门

POST `/api/v1/delivery/orders/{orderId}/compartment/open`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| action | string | 是 | put_item、take_item |
| verifyCode | string | 否 | 取件码/验证码 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| compartmentNo | string | 舱门编号 |
| message | string | 提示信息 |

### 3.9 确认放件/取件

POST `/api/v1/delivery/orders/{orderId}/confirm`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| action | string | 是 | put_item、take_item |

## 4. 订单状态定义

### 4.1 业务订单状态

| 状态 | 说明 |
| --- | --- |
| pending_payment | 待支付 |
| paid | 已支付/待调度 |
| dispatching | 调度中 |
| robot_assigned | 已分配机器人 |
| going_to_pickup | 前往取件点 |
| waiting_put_item | 等待放件 |
| delivering | 配送中 |
| waiting_take_item | 等待取件 |
| completed | 已完成 |
| canceled | 已取消 |
| failed | 已失败 |
| exception | 异常处理中 |

### 4.2 机器人任务状态

| 状态 | 说明 |
| --- | --- |
| created | 任务已创建 |
| assigned | 已分配机器人 |
| to_pickup | 前往取件点 |
| arrived_pickup | 已到达取件点 |
| loaded | 已放件 |
| to_delivery | 前往收件点 |
| arrived_delivery | 已到达收件点 |
| unloaded | 已取件 |
| finished | 任务完成 |
| canceled | 任务取消 |
| failed | 任务失败 |
| abnormal | 任务异常 |

## 5. 特惠购卡

### 5.1 查询特惠卡列表

GET `/api/v1/benefit-cards`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| list | array | 特惠卡列表 |

卡字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| cardId | string | 卡 ID |
| name | string | 卡名称 |
| description | string | 描述 |
| price | number | 售价 |
| originalPrice | number | 原价 |
| validDays | integer | 有效天数 |
| benefits | array | 权益列表 |
| status | string | 状态 |

### 5.2 创建购卡订单

POST `/api/v1/benefit-card-orders`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| cardId | string | 是 | 特惠卡 ID |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| orderId | string | 订单 ID |
| orderNo | string | 订单编号 |
| payableAmount | number | 应付金额 |
| status | string | 订单状态 |

### 5.3 查询我的特惠卡

GET `/api/v1/user/benefit-cards`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| list | array | 用户已购卡列表 |

### 5.4 查询待支付购卡订单

GET `/api/v1/benefit-card-orders/pending`

## 6. 积分兑换

### 6.1 查询积分账户

GET `/api/v1/points/account`

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| availablePoints | integer | 可用积分 |
| frozenPoints | integer | 冻结积分 |
| expiringPoints | integer | 即将过期积分 |

### 6.2 查询积分商品列表

GET `/api/v1/points/products`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pageNo | integer | 否 | 页码 |
| pageSize | integer | 否 | 每页数量 |

### 6.3 查询积分商品详情

GET `/api/v1/points/products/{productId}`

### 6.4 创建积分兑换订单

POST `/api/v1/points/exchange-orders`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| productId | string | 是 | 商品 ID |
| quantity | integer | 是 | 数量 |
| receiverName | string | 否 | 收件人 |
| receiverPhone | string | 否 | 手机号 |
| receiverAddress | string | 否 | 收件地址 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| exchangeOrderId | string | 兑换订单 ID |
| pointsCost | integer | 消耗积分 |
| status | string | 状态 |

## 7. 我的订单

### 7.1 查询订单列表

GET `/api/v1/orders`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 否 | delivery、benefit_card、points |
| statusGroup | string | 否 | all、pending_payment、pending_receipt |
| pageNo | integer | 否 | 页码 |
| pageSize | integer | 否 | 每页数量 |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| list | array | 订单列表 |
| total | integer | 总数 |

### 7.2 查询统一订单详情

GET `/api/v1/orders/{orderId}`

响应字段按订单类型返回配送订单、特惠卡订单或积分兑换订单详情。

### 7.3 待支付订单支付

POST `/api/v1/orders/{orderId}/pay`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| payChannel | string | 是 | 支付渠道：wechat、alipay、balance |

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| paymentId | string | 支付单 ID |
| payParams | object | 支付参数 |

### 7.4 新任务发起反馈

POST `/api/v1/feedback/new-task`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| sourceOrderId | string | 否 | 来源订单 ID |
| content | string | 是 | 反馈内容 |
| images | array | 否 | 图片地址 |
| contactPhone | string | 否 | 联系电话 |

## 8. 支付回调

### 8.1 支付结果回调

POST `/api/v1/payments/callback`

请求字段由支付渠道决定，系统需完成签名校验、订单状态更新、权益发放或配送任务发起。

## 9. 机器人回调

### 9.1 机器人任务状态回调

POST `/api/v1/robot/callbacks/task-status`

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| robotTaskId | string | 是 | 机器人任务 ID |
| orderNo | string | 否 | 我方订单编号 |
| robotId | string | 是 | 机器人 ID |
| status | string | 是 | 机器人任务状态 |
| location | object | 否 | 机器人当前位置 |
| eventTime | string | 是 | 事件时间 |
| reasonCode | string | 否 | 异常/失败原因编码 |
| reasonText | string | 否 | 异常/失败原因描述 |

响应：

```json
{
  "code": 0,
  "message": "success"
}
```

## 10. 后台管理接口

### 10.1 查询机器人列表

GET `/api/admin/v1/robots`

### 10.2 查询机器人任务列表

GET `/api/admin/v1/robot-tasks`

### 10.3 人工取消配送任务

POST `/api/admin/v1/delivery/orders/{orderId}/cancel`

### 10.4 人工重新调度

POST `/api/admin/v1/delivery/orders/{orderId}/redispatch`

### 10.5 人工开舱

POST `/api/admin/v1/delivery/orders/{orderId}/compartment/open`

### 10.6 查询异常订单

GET `/api/admin/v1/delivery/orders/exceptions`

## 11. 关键业务规则

1. 创建配送订单需要保证幂等，同一个 `X-Request-Id` 不得重复创建订单。
2. 配送任务状态以机器人厂商回调为准，同时系统需支持主动查询补偿。
3. 取消订单需要根据任务状态判断是否允许取消以及是否扣费。
4. 打开舱门、确认放件、确认取件均需记录操作日志。
5. 支付成功后，若订单为配送订单，应自动发起机器人调度；若为特惠卡订单，应自动发放权益。
6. 机器人异常时，订单进入 `exception`，由后台人工处理或自动重新调度。
7. 我的订单列表应聚合配送订单、特惠卡订单和积分兑换订单。

